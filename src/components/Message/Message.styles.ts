import { styled } from "utils/stitches.config";

export const Container = styled("div", {
  borderRadius: 12,
  background: "$white",
  padding: 16,
  display: "flex",
  gap: "8px",
  flexDirection: "column",
});
