import { styled } from "utils/stitches.config";

const Heading = styled("h2", {
  fontFamily: "Poppins",
  fontSize: "$large",
  fontWeight: 600,
  lineHeight: "27px",
  margin: 0,
});

export default Heading;
