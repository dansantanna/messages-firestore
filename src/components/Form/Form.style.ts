import { styled } from "@stitches/react";

export const Container = styled("form", {
  display: "flex",
  flexDirection: "column",
  marginBottom: 24,
  padding: "0 16px",
  h2: { marginBottom: 8 },
  button: { marginTop: 16 },
});
